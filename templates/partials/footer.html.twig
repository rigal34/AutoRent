<style>
    body {
        padding-top: 80px !important;
    }

    .navbar-prestige {
        background: linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%) !important;
        border-bottom: 2px solid #ffc107;
        box-shadow: 0 2px 15px rgba(255, 193, 7, 0.2);
        transition: all 0.3s ease;
        /* 🎯 LARGEUR RÉDUITE POUR ALIGNER AU HEADER */
        margin: 0 auto;
        max-width: calc(100% - 24px); /* Réduction de 12px de chaque côté */
    }

    .navbar-prestige .nav-link:hover {
        color: #ffc107 !important;
        transition: color 0.3s ease;
    }

    .navbar-brand {
        color: #ffc107 !important;
        font-weight: bold !important;
    }

    /* 🚀 FOOTER ALIGNÉ COMME LA NAVBAR */
    .footer-prestige {
        /* 🎯 MÊME PRINCIPE QUE LA NAVBAR */
        margin: 0 auto;
        max-width: calc(100% - 24px); /* Même réduction que navbar */
        
        box-shadow: 0 -2px 15px rgba(255, 193, 7, 0.2); /* Ombre vers le haut */
        border-top: 2px solid #ffc107; /* Bordure dorée en haut */
    }

    /* 🎨 RESPONSIVE POUR MOBILE */
    @media (max-width: 768px) {
        .navbar-prestige, .footer-prestige {
            max-width: calc(100% - 16px); /* Moins de réduction sur mobile */
            margin: 0 8px;
        }
    }
</style>

<footer class="bg-dark shadow-sm footer-prestige">
  <div class="container py-2 my-1">
    <div class="d-flex justify-content-between align-items-center">
      <!-- Gauche -->
       <span class="text-white me-auto">© 2025 Company, Inc</span>

      <!-- Droite : Accueil + Instagram + Facebook -->
      <nav class="d-flex align-items-center gap-3">
        <a href="{{ path('app_home') }}" class="text-white text-decoration-none">Accueil</a>

        <!-- Instagram -->
        <a href="https://www.instagram.com/" class="text-white icon-link" aria-label="Instagram">
          <svg viewBox="0 0 24 24" width="22" height="22" aria-hidden="true">
            <rect x="3" y="3" width="18" height="18" rx="4" ry="4"
                  fill="none" stroke="currentColor" stroke-width="2"/>
            <circle cx="12" cy="12" r="5"
                    fill="none" stroke="currentColor" stroke-width="2"/>
            <circle cx="17.5" cy="6.5" r="1.4" fill="currentColor"/>
          </svg>
        </a>

        <!-- Facebook -->
        <a href="https://www.facebook.com/" class="text-white icon-link" aria-label="Facebook">
          <svg viewBox="0 0 24 24" width="22" height="22" aria-hidden="true">
            <path fill="currentColor"
              d="M22 12a10 10 0 1 0-11.6 9.9v-7h-2.3V12h2.3V9.6c0-2.2 1.3-3.6 3.5-3.6c1 0 2 .1 2 .1v2.2h-1.1
                 c-1.1 0-1.5.7-1.5 1.5V12h2.6l-.4 2.9H13.3v7A10 10 0 0 0 22 12z"/>
          </svg>
        </a>
      </nav>
    </div>
  </div>
</footer>

