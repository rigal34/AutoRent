
<link rel="stylesheet" href="{{ asset('css/navbar.css') }}">
<link rel="stylesheet" href="{{ asset('css/responsive.css') }}">

<nav class="navbar navbar-expand-lg navbar-dark fixed-top navbar-prestige">
    <div class="container-fluid">
        <!-- Logo positionné à gauche -->
        <a class="navbar-brand" href="{{ path('app_home') }}"> AutoRent</a>

        <!-- Bouton burger -->
        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarNav"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Contenu du menu -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <!-- Menu principal centré -->
            <ul class="navbar-nav mx-auto">
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_vehicules_index') }}">Notre Catalogue</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_actualite') }}">Actualité</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_contact') }}">Contact</a>
                </li>
            </ul>

            <!-- Boutons à droite -->
            <div class="btn-container d-flex gap-2">
                {% if app.user %}
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-warning">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-warning">Connexion</a>
                    <a href="{{ path('app_register') }}" class="btn btn-warning">S'inscrire</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>



{# //////////////////////////////////////FIN POUR LA NAVBAR//////////////////////////////////////////////////////////////////// #}
{# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ #}
{# ::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::: #}
<!--  SCRIPT POUR L'EFFET SCROLL -->



<script>
    //  Protection contre les exécutions multiples
    if (!window.navbarScrollInitialized) {
        window.navbarScrollInitialized = true;
        
        document.addEventListener('DOMContentLoaded', function() {
            console.log('🎉 DOM est prêt depuis navbar.html.twig !');

            window.addEventListener('scroll', function() {
                const navbar = document.querySelector('.navbar-prestige');

                if (navbar) {
                    if (window.scrollY > 50) {
                        navbar.style.background = 'rgba(15, 15, 15, 0.95)';
                        navbar.style.boxShadow = '0 4px 25px rgba(255, 193, 7, 0.4)';
                        console.log('📜 Scroll détecté !');
                    } else {
                        navbar.style.background = 'linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%)';
                        navbar.style.boxShadow = '0 2px 15px rgba(255, 193, 7, 0.2)';
                        console.log('🔝 Retour en haut !');
                    }
                } else {
                    console.log('❌ Navbar non trouvée !');
                }
            });
        });
    }
</script>

{# :::::::::::::::FIN DE SCRIPT POUR L'EFFET SCROLL:::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::: #}