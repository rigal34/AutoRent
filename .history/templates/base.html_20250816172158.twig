<!DOCTYPE html>
<html>
    <head>
        {# ... ton code <head> ... #}
    </head>
    
    <body class="d-flex flex-column vh-100">
    
        <header>
            {% include 'partials/navbar.html.twig' %}
        </header>
    
        {# 1. On définit la pièce de vie principale de la maison #}
        <main class="flex-grow-1">
            <div class="container"> {# On ajoute un container pour centrer le contenu #}

                {# 2. On installe la boîte aux lettres au début de la pièce #}
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }} text-center mt-3" role="alert">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}

                {# 3. C'est ici que le contenu spécifique de chaque page (le "mobilier") viendra se placer #}
                {% block body %}{% endblock %}

            </div>
        </main>
        
        <footer class="mt-auto">
            {% include 'partials/footer.html.twig' %}
        </footer>
        
    </body>
</html>