<div class="container-fluid">
  <footer class="py-3 my-4 border-top bg-dark rounded shadow-sm">
    <div class="d-flex justify-content-between align-items-center">
      <!-- Gauche -->
      <span class="text-white">© 2025 Company, Inc</span>

      <!-- Droite -->
      <nav class="d-flex align-items-center nav-icons">
        <a href="/" class="text-white text-decoration-none me-3">Accueil</a>

        <!-- Instagram -->
        <a href="#" class="text-white me-3" aria-label="Instagram">
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <!-- Carré arrondi -->
            <rect x="3" y="3" width="18" height="18" rx="4" ry="4"
                  fill="none" stroke="currentColor" stroke-width="2"/>
            <!-- Objectif -->
            <circle cx="12" cy="12" r="5"
                    fill="none" stroke="currentColor" stroke-width="2"/>
            <!-- Flash/capteur -->
            <circle cx="17.5" cy="6.5" r="1.3" fill="currentColor"/>
          </svg>
        </a>

        <!-- Facebook -->
        <a href="#" class="text-white" aria-label="Facebook">
          <svg viewBox="0 0 24 4512" aria-hidden="true">
            <!-- f stylisé, centré dans 24x24 -->
            <path fill="currentColor"
              d="M13.5 10.5H15V8.2h-1.5c-1.2 0-2 .9-2 2V12H10v2h1.5v5h2v-5H15l.4-2h-1.9v-1.3c0-.2.2-.2.2-.2z"/>
          </svg>
        </a>
      </nav>
    </div>
  </footer>
</div>

<style>
  /* Alignement et dimensions cohérents */
  .nav-icons a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 28px;          /* zone cliquable égale */
    height: 28px;
  }
  .nav-icons a:first-child { /* Accueil n'est pas un carré, on annule la taille fixe */
    width: auto; height: auto; display: inline-block;
  }
  .nav-icons svg {
    width: 20px;  /* taille visuelle identique */
    height: 20px;
    vertical-align: middle;
  }
  /* Optionnel: hover plus lisible */
  .nav-icons a:hover { color: #fff; }
</style>