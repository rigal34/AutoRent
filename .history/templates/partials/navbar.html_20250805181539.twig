<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AutoRent</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar {
            background-color: white;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
            position: relative;
            padding: 1rem 0;
        }
        .nav-link {
            color: #0d6efd;
            font-weight: 500;
            padding: 0 1.5rem; /* Augmentation de l'espacement horizontal */
            margin: 0 0.5rem; /* Ajout de marges supplémentaires */
        }
        .btn-outline-primary {
            border-color: #0d6efd;
            color: #0d6efd;
            margin-left: 1rem;
        }
        .btn-outline-primary:hover {
            background-color: #0d6efd;
            color: white;
        }
        .navbar-brand {
            position: absolute;
            left: 2rem;
            font-size: 1.5rem;
            font-weight: bold;
        }
        .navbar-nav {
            margin: 0 auto;
        }
        .navbar-toggler {
            position: absolute;
            right: 1rem;
        }
        @media (min-width: 992px) {
            .navbar-toggler {
                display: none;
            }
            .nav-item:not(:last-child) {
                margin-right: 2rem; /* Espacement plus important entre les éléments */
            }
        }
        .btn-container {
            margin-left: 2rem;
        }
        .btn {
            padding: 0.5rem 1.5rem;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
            <a class="navbar-brand" href="#">AutoRent</a>

            <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="#">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Nos Véhicules</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Contact</a>
                    </li>
                </ul>
                <div class="btn-container d-flex">
                    <a href="#" class="btn btn-outline-primary me-3">Connexion</a>
                    <a href="#" class="btn btn-primary">S'inscrire</a>
                </div>
            </div>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
    </nav>

   

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>